interface:
  display_name: "Implementation Skill"
  short_description: "Reviewed and verified Wirelog implementation workflow"
  default_prompt: "Use /dev-workflows:implementation-skill to select an atomic workflow with Wirelog, then test it, obtain independent review and Architect/Critic approval, and create the verified atomic commit."
