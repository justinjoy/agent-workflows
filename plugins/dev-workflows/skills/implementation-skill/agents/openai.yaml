interface:
  display_name: "Implementation Skill"
  short_description: "Wirelog-selected implementation workflow"
  default_prompt: "Use $dev-workflows:implementation-skill to select an atomic workflow with Wirelog, then implement and validate this change."
